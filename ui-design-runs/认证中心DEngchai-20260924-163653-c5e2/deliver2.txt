⚠  这一稿还差 1 件事。**交付参数照出**，但你应该在宣布做完之前先看一眼：

   · 看图只有 4 轮（且最后一轮没有一条标 resolved:true） —— 第二轮的意义是复核第一轮说要改的地方改了没有。
      改完再跑一次：python3 C:\Users\36434\AppData\Local\DoubaoWork\User Data\Profile 2\.doubaowork\agent_mode\workspace\.skills\doubao-ui-design\scripts\ui.py review --run D:\Engchain3.0\ui-design-runs\认证中心DEngchai-20260924-163653-c5e2\run.json

[
  {
    "step": 1,
    "tool": "NotifyHuman",
    "why": "Doc 卡必须最先交；顺序换了第二张卡会把它挤掉",
    "args": {
      "attachments": [
        {
          "attachment_name": "认证中心（D:/Engchain3.0/… · 设计方案",
          "attachment_source": "https://bytedance.larkoffice.com/docx/ZozydGT8VoK28oxj3bBcpRlenic"
        }
      ]
    }
  },
  {
    "step": 2,
    "tool": "app_builder_agent",
    "why": "网页/H5 类产物严禁走 NotifyHuman；它自己上屏，不再 NotifyHuman",
    "args": {
      "arch_type": "jspage",
      "user_prompt": "↓↓ 见下面 48499 字的整段正文，原样复制 ↓↓"
    }
  }
]

========================================================================
app_builder_agent 的 user_prompt 已备好：D:\Engchain3.0\ui-design-runs\认证中心DEngchai-20260924-163653-c5e2\deliver\app-prompt.txt
  体积 47KB（48499 字，含完整成品 HTML，图片是公网 URL）
  **用 Read 读这个文件，把内容整段作为 user_prompt。**
  ✗ 不要传文件路径给 app_builder（它的沙箱读不到本机文件，只能照剩下的描述重画，图会全部丢失）
  ✗ 不要写功能摘要，✗ 不要按自己的理解重述
========================================================================

**上面两步都要做，一步都不能少。**
  ① NotifyHuman 交 Doc URL —— 少了它，用户拿不到设计说明
  ② app_builder_agent 交界面卡 —— 少了它，用户点不开你的界面
  顺序不能换：网页卡自己上屏，它之后 Doc 卡会被挤掉。

整行复制上面的 attachment_source，不要重打。
调用完把返回原文贴回来对账：
  python3 C:\Users\36434\AppData\Local\DoubaoWork\User Data\Profile 2\.doubaowork\agent_mode\workspace\.skills\doubao-ui-design\scripts\deliver.py --run D:\Engchain3.0\ui-design-runs\认证中心DEngchai-20260924-163653-c5e2\run.json --confirm "<返回原文>"

**交完卡再回读一次**（这是唯一能证明卡片里是这一稿的动作）：
  拿返回的 app_id 再调一次 app_builder_agent，问：
    「这个应用首屏有几个 <img>？它们的 src 分别是什么？」
  · 期望：>= 5 个，且 src 全是 https:// 或 data:
  · 实测 0 个或远少于 5 → 它没用你给的 HTML，是照描述重画的。
    把 D:\Engchain3.0\ui-design-runs\认证中心DEngchai-20260924-163653-c5e2\deliver\app-prompt.txt 整段重发一次（带同一个 app_id，编辑模式）。
