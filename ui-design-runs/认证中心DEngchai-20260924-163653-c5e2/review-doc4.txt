══════ 合同 · 每条命令都重印（上下文会被压缩，别靠记）══════════════════
原话  认证中心（D:/Engchain3.0/pages/profile/…深度设计与优化。需要重视用户体验，视觉感受。
合同  mobile-app · revise · 产品界面 · 交 html+doc
风格  saturated / sans-cool / tri-role　← 配色/字体/构图按它长，不要凭手感
阶段  文档（doc/canvas-a.html、doc/canvas-b.html）
交件① app/index.html        版心在 1440 下必须 <=520px（取景框）· 量 390×844 与 1440×900（卡片被看到的宽度）
交件② doc/*                 画布 2 块 + 封面 + proposal　关键界面节由 ship 自动写入
顺序  先做完①再写② —— ②的关键界面节放的是①的真机截图，不是素材照片
下一步  看图＝**执行** ui.py review（不是自己 open_url/截图/起 http.server）　交付＝ui.py ship 出参数：先 NotifyHuman 交 Doc、再 app_builder_agent 交卡
对手  C:\Users\36434\AppData\Local\DoubaoWork\User Data\Profile 2\.doubaowork\agent_mode\workspace\.skills\doubao-ui-design\gold\app-home\index.html　← 学机制不抄版式（gold/README.md）
盘在  D:\Engchain3.0\ui-design-runs\认证中心DEngchai-20260924-163653-c5e2
══════════════════════════════════════════════════════════════════════════

──────────────────────────────────────────────────────────────────────
①  建骨架（缺什么建什么，已有的不覆盖）
──────────────────────────────────────────────────────────────────────

──────────────────────────────────────────────────────────────────────
②  封面出图 + 画布看自己 + 门
──────────────────────────────────────────────────────────────────────
saved shots/cover.jpg
**把 D:\Engchain3.0\ui-design-runs\认证中心DEngchai-20260924-163653-c5e2\shots\cover.jpg 读进上下文看一眼** —— 封面是客户打开看到的第一屏。

封面进飞书还差一个公网 URL（本地路径会被静默丢弃）：
  1 调用宿主工具：
      FileBatchUpload(path_list=["D:\Engchain3.0\ui-design-runs\认证中心DEngchai-20260924-163653-c5e2\shots\cover.jpg"])
  2 把返回的 uri 回填给发布：
      python3 C:\Users\36434\AppData\Local\DoubaoWork\User Data\Profile 2\.doubaowork\agent_mode\workspace\.skills\doubao-ui-design\scripts\publish.py --run D:\Engchain3.0\ui-design-runs\认证中心DEngchai-20260924-163653-c5e2\run.json --cover-url https://aka.doubaocdn.com/s/xxxx
saved shots/canvas-a-698.png
  PASS  img-broken            贴进来的图都解码且渲染 >8px
  PASS  img-upscaled          原始像素 >= 渲染宽度（目标 2.0×）；pack_img.py 会在宽度和质量下限内协商
  PASS  text-invisible        >=3.0:1（背景可信的文字）
  PASS  font-too-small        无 <10px 正文
  PASS  unit-too-small        单位标签 >=14px（有就查，没有跳过）
  PASS  overflow-x            <=1px
  PASS  placeholder           无占位符（含图位说明：脚本给的那句话要换成你自己的）
  PASS  hit-area              可点元素 >=24px
  PASS  element-overlap       同层文字元素两两不相交
  PASS  reading-gutter        正文左右离版心边 >=16px（padding 简写会把 padding-inline 清零，要看实测不看声明）
  PASS  dock-at-bottom        常驻底栏的下边缘 = 视口底（sticky 会被容器 padding 顶高，用 absolute/fixed）
  PASS  doc-layout-repeat     相邻 3 节结构指纹不得全同（同构 = 退化成模板）
  PASS  device-scale          竖图（手机屏）在画布里渲染宽 <=版心 40% —— 手机要有机身和场，参考图实测机身占 15–26%
  PASS  doc-image-aspect      内嵌图高宽比 <=2.5 —— 整页长图切段（pack_img.py 会自动切）或改用元素级截图 pg.locator(".band").screenshot()
  PASS  doc-screens-empty     关键界面的图位里要有图 —— 空槽位说明成品页的截图没进来。 `ui.py ship` 的第①段会自动把真机截图写进 KEY-SCREENS 标记块，写进去的片段自带样式；报这一条说明标记块与图位容器都不在了。
  PASS  orphan-row            每个等分网格的列数整除项数（否则出现 3+1 / 5+1 孤儿行，整版立刻显乱）
  PASS  grid-declared-vs-actual声明的 --cols 与实际渲染列数一致，且网格类名要真的有对应规则
  PASS  gutter-balance        画布上限与正文行宽要匹配，右侧余量不该单独堆积
  PASS  sample-wrap           字阶/色板示例不许折行（加 white-space:nowrap）
  PASS  doc-reading-axis      主轴正文行宽 20–46 个中文字（换算 45–75 西文字符）
  PASS  doc-persona-clone     与任何一份 gold 相同的视觉角色 <=5（三份 gold 两两之间实测最多 5）
  PASS  js-error              零 JS 报错

22/22 通过｜BLOCK 0｜REPORT 0
saved shots/canvas-b-698.png
  PASS  img-broken            贴进来的图都解码且渲染 >8px
  PASS  img-upscaled          原始像素 >= 渲染宽度（目标 2.0×）；pack_img.py 会在宽度和质量下限内协商
  PASS  text-invisible        >=3.0:1（背景可信的文字）
  PASS  font-too-small        无 <10px 正文
  PASS  unit-too-small        单位标签 >=14px（有就查，没有跳过）
  PASS  overflow-x            <=1px
  PASS  placeholder           无占位符（含图位说明：脚本给的那句话要换成你自己的）
  PASS  hit-area              可点元素 >=24px
  PASS  element-overlap       同层文字元素两两不相交
  PASS  reading-gutter        正文左右离版心边 >=16px（padding 简写会把 padding-inline 清零，要看实测不看声明）
  PASS  dock-at-bottom        常驻底栏的下边缘 = 视口底（sticky 会被容器 padding 顶高，用 absolute/fixed）
  PASS  doc-layout-repeat     相邻 3 节结构指纹不得全同（同构 = 退化成模板）
  PASS  device-scale          竖图（手机屏）在画布里渲染宽 <=版心 40% —— 手机要有机身和场，参考图实测机身占 15–26%
  PASS  doc-image-aspect      内嵌图高宽比 <=2.5 —— 整页长图切段（pack_img.py 会自动切）或改用元素级截图 pg.locator(".band").screenshot()
  PASS  doc-screens-empty     关键界面的图位里要有图 —— 空槽位说明成品页的截图没进来。 `ui.py ship` 的第①段会自动把真机截图写进 KEY-SCREENS 标记块，写进去的片段自带样式；报这一条说明标记块与图位容器都不在了。
  PASS  orphan-row            每个等分网格的列数整除项数（否则出现 3+1 / 5+1 孤儿行，整版立刻显乱）
  PASS  grid-declared-vs-actual声明的 --cols 与实际渲染列数一致，且网格类名要真的有对应规则
  PASS  gutter-balance        画布上限与正文行宽要匹配，右侧余量不该单独堆积
  PASS  sample-wrap           字阶/色板示例不许折行（加 white-space:nowrap）
  PASS  doc-reading-axis      主轴正文行宽 20–46 个中文字（换算 45–75 西文字符）
  PASS  doc-persona-clone     与任何一份 gold 相同的视觉角色 <=5（三份 gold 两两之间实测最多 5）
  PASS  js-error              零 JS 报错

22/22 通过｜BLOCK 0｜REPORT 0

**画布也走看图闭环**：把上面两张 698 宽的图读进来看一遍。文档那一半的缺陷同样只有看图才发现得了。

══════ 合同 · 每条命令都重印（上下文会被压缩，别靠记）══════════════════
原话  认证中心（D:/Engchain3.0/pages/profile/…深度设计与优化。需要重视用户体验，视觉感受。
合同  mobile-app · revise · 产品界面 · 交 html+doc
风格  saturated / sans-cool / tri-role　← 配色/字体/构图按它长，不要凭手感
阶段  文档（doc/canvas-a.html、doc/canvas-b.html）
交件① app/index.html        版心在 1440 下必须 <=520px（取景框）· 量 390×844 与 1440×900（卡片被看到的宽度）
交件② doc/*                 画布 2 块 + 封面 + proposal　关键界面节由 ship 自动写入
顺序  先做完①再写② —— ②的关键界面节放的是①的真机截图，不是素材照片
下一步  看图＝**执行** ui.py review（不是自己 open_url/截图/起 http.server）　交付＝ui.py ship 出参数：先 NotifyHuman 交 Doc、再 app_builder_agent 交卡
对手  C:\Users\36434\AppData\Local\DoubaoWork\User Data\Profile 2\.doubaowork\agent_mode\workspace\.skills\doubao-ui-design\gold\app-home\index.html　← 学机制不抄版式（gold/README.md）
盘在  D:\Engchain3.0\ui-design-runs\认证中心DEngchai-20260924-163653-c5e2
══════════════════════════════════════════════════════════════════════════

──────────────────────────────────────────────────────────────────────
NEXT ▸ 发布 + 出交付参数（发过一次就自动改成重发同一篇，不会产生第二个链接）

    python3 C:\Users\36434\AppData\Local\DoubaoWork\User Data\Profile 2\.doubaowork\agent_mode\workspace\.skills\doubao-ui-design\scripts\ui.py ship --run D:\Engchain3.0\ui-design-runs\认证中心DEngchai-20260924-163653-c5e2\run.json
──────────────────────────────────────────────────────────────────────
