工作目录 → D:\Engchain3.0\ui-design-runs\聊天窗口DEngchai-20260924-110626-0341
            （你指定的 --dir；产物、收据、合同全在这里，不在 skill 安装目录）

──────────────────────────────────────────────────────────────────────
①  交付合同（冻结之后就不改了）
──────────────────────────────────────────────────────────────────────
合同已冻结 → D:\Engchain3.0\ui-design-runs\聊天窗口DEngchai-20260924-110626-0341\run.json

合同四字段（判错了就改，改一个不影响另外三个）：
  谁说的  provenance   explicit    用户点名了 —— 本包的默认与偏好全部让位
  做什么  operation    revise      按用户给的旧版改，默认全部保留，只改点名的那几处；doc 要有改前改后对比
  长什么样 surface      mobile-app  **真实产品界面原型**：首屏就是用户日常会看到的那一屏，不是关于它的说明
  画布    canvas       390×844    来自 载体默认　← **门在这个尺寸量，图也在这个尺寸截**
  交什么  handoff      html+doc    两件必交，缺一不算完成

需求账本（用户逐字点名的 2 项，ship 会逐条对账）：
  ● 文案   发名片
  ○ 必须   优化信息来源和卡片的UI　（只报告，不阻断）
  ● 的必须**逐字**出现在产物里 —— 用户写「立即预约」就不能写「马上预约」。

载体：web（宽屏）　形态：**产品界面**
必交 ①  设计方案 Doc：doc/canvas-a.html, doc/canvas-b.html + doc/cover.html + doc/proposal.xml
        封面由 `ui.py review`（文档阶段）出图、`ui.py ship` 插进第 1 块，插完回读确认，不成换定位再来，最多三级 —— 你不用记这两步。
        （封面**不能**写进 proposal 的 <img href>：飞书只吃公网 URL，本地路径被静默丢弃且不报错）
必交 ②  **真实产品界面原型**：首屏就是用户日常会看到的那一屏，不是关于它的说明：app/index.html

**两件必交有先后**：doc 的「关键界面」那一节放的是**成品页的真机截图**，
所以顺序是 —— 先把 app/index.html 做完、看过、改好，再回来写 doc。

  1  做成品页 → shoot.py full / screens → observe.py 看图改稿
  2  这三步 `ui.py ship` 自己做完：整页长图 → 压缩 → 转 data URI
  3  → 原地写进画布的 <!-- KEY-SCREENS-START --> 标记块
     宽屏稿的证据是**通栏长图**（.plate），不是手机机身位 —— ship 按载体分好了
  4  这时才写 doc/canvas-*.html 的关键界面节

  **那一节不放素材照片。** 实测一份真实产物把咖啡馆场景照放进了「首页改版全貌」，
  另一份把 .screens 容器建好了却一张图都没填 —— 两份的关键屏截图都躺在 shots/ 里。

「产品界面」的三条正向口径（门会逐条量，缺一条就重出）：
  1. 首屏 = 产品首页本身。第一眼看到的就是产品，不是封面、不是标题页
  2. 至少 2 个由 JS 切换的真实界面状态（切 tab / 点进详情 / 展开卡片 / 下拉刷新），点了真的变
  3. 不出现幻灯片语汇：标题里 0 处「方案 / 设计 / 改版 / 全貌 / 总结 / 封面 / 结束」——那些属于 doc，不属于原型
  —— 判错这一条的代价是最大的：典型失败是把一个「小程序首页改版」
     做成了 5 屏宣讲 deck，第 4 屏叫「完整首页 · 改版后全貌」，
     里面贴了一张 400px 的缩略图。那是严重缺陷。
     形态判错了就换：python3 C:\Users\36434\AppData\Local\DoubaoWork\User Data\Profile 2\.doubaowork\agent_mode\workspace\.skills\doubao-ui-design\scripts\ui.py brief --force --shape 单向叙事 --query "…"

素材计划（4 个角色，成品页最少 5 个真实视觉资产）：
  · hero-bg          image_gen     full-bleed  首屏满版氛围，文字压在上面
  · icon-set         svg           inline      图标必须用 SVG——生图出不了透明底
      包里有 Phosphor 全套（MIT，1512 个，6 种字重，fill=currentColor，viewBox 0 0 256 256）：
        python3 C:\Users\36434\AppData\Local\DoubaoWork\User Data\Profile 2\.doubaowork\agent_mode\workspace\.skills\doubao-ui-design\scripts\icons.py find 搜索 消息 发布 我的
      ← 直接打印可内联的 <svg>。**不要手搓 path，也不要用 emoji。**
  · texture          css           layer       底纹用 CSS 画，不占素材预算
  · scene-01         image_gen     full-bleed  一张能定调的场景

两条都是必交，缺一不算完成——publish.py 会核对。

必读五件套（动手之前读完，约 7 秒；不是"按需"）：
  craft/award-mechanisms.md      什么叫得奖级 —— 未读它之前不要开始定风格
  craft/scale-atlas.md           12 份获奖稿的数值**区间** —— 未读它之前不要定字阶和圆角
  craft/composition-motion.md    构图与动效的**关系** —— 未读它之前不要决定首屏长什么样
  craft/aesthetic-baseline.md    四组数值的判据 —— 未读它之前不要写 :root
  method/04-build-web.md        这个载体怎么做 —— 未读它之前不要拷骨架
  reference/image-direction.md   生图门 A 八项 —— 采素材之前读

  不要写成"三份就够开工，其余按需"，并许诺"脚本会打印给你"。
  那个通路根本不存在。工艺必须有一条会被真的走的路，这就是那条路。

本载体的参考区间（**区间不是目标值**，落在哪一段由签名定）：
  字阶断崖 H1÷正文   p10 2.8 / 中位 3.6 / p90 6.9
  正文字号          p10 14 / 中位 16 / p90 20
  圆角              获奖库中位 3px，12 份里 4 份纯直角
                    —— 常见产物的实测中位是 50px（全是胶囊），这是差得最远的一项
  强调色饱和度       中位 0.80，12 份里 10 份 >=0.6

  **这一稿的对手不是获奖 H5，是用户手机里那个真的 App。**
    参照 gold/app-home/（真实产品首页原型，带 tab 切换与详情态），
    交互工艺见 craft/interaction-contract.md —— 那九条讲的是
    「控件和内容怎么响应状态」，与题材无关，产品界面同样适用。

这一稿的对手 —— **先看它，再动手**。它是真做出来、被验收通过的，带完整源码。

  C:\Users\36434\AppData\Local\DoubaoWork\User Data\Profile 2\.doubaowork\agent_mode\workspace\.skills\doubao-ui-design\gold\app-home\index.html
    「班次」通勤到站 App 首页 · 能用的产品界面原型
    ▸ 为什么是这一份：你这一稿是「产品界面」，包里 8 组 gold 里只有它对口
    ▸ L117 起 —— 滚动容器怎么给底栏和安全区预留位置 —— 你会遇到同一个几何问题
      #flow{position:relative;z-index:10;height:100%;overflow-y:auto;
        -webkit-overflow-scrolling:touch;scrollbar-width:none;
        padding:calc(env(safe-area-inset-top) + 152px) 20px
                calc(var(--tab) + env(safe-area-inset-bottom) + 26px)}
      #flow::-webkit-scrollbar{display:none}
      .panel[hidden]{display:none}
    ▸ L154 起 —— 信息块的密度与缝隙 —— 为什么它不像卡片墙
      .plan{background:var(--surface);border:1px solid var(--line);border-radius:var(--r);
        padding:14px 16px;margin-bottom:10px}
      .plan .hd{display:flex;align-items:center;gap:9px}
      .plan .tag{font-size:11.5px;font-weight:700;padding:3px 7px;border-radius:5px;
        color:#08120C;background:var(--line4)}
      .plan .tag.b{background:var(--line2);color:#060C1E}
    ▸ L495 起 —— tab 切换的状态机 —— prototype-not-deck 要的「点了真的变」
      tabs.forEach(b=>b.onclick=()=>{
        tabs.forEach(x=>{const on=x===b;
          x.classList.toggle('on',on);x.setAttribute('aria-selected',String(on));});
        panels.forEach(p=>{p.hidden=p.dataset.panel!==b.dataset.go;});
        document.getElementById('flow').scrollTop=0;
      });

    规矩（gold/README.md 原文）：**学机制，不抄版式。**
      允许学：为什么章节用一条墨线分隔、为什么商品缝隙只留一线。
      禁止抄：四宫格的具体排法、关键屏网格的具体结构。
      **产物和 gold 在版式上一眼能认出是同一套模板，就是抄了。**

  外部锚点（reference/fingerprints/index.json，2026-07-25 真实浏览器实测，不是估计值）：
    cowboy   满版真实照片 + 巨字标题 + 单个胶囊 CTA + 近单色深场
             字阶 4.5 · display 72 · 首屏图占比 100% · 面色 3 种
    lunge    近白场 + 一张巨大产品照居中 + 左下极小数据对（产地/价格）+ 一个黑胶囊 CTA；留白本身就是设计
             字阶 2.25 · display 36 · 首屏图占比 100% · 面色 2 种
    fuse     近白场 + 居中一句大标题 + 一行副标 + 一个黑按钮 + 一张真实场景照（水泥地上的手机）；无 feature 三卡、无玻璃卡
             字阶 4.0 · display 64 · 首屏图占比 46.4% · 面色 1 种

  ▸ 动手之前写下一条：**这一稿要在哪一维超过它们？** 指到具体机制，不要写「更好看」。

──────────────────────────────────────────────────────────────────────
②  风格签名（决定这一稿往哪个方向长）
──────────────────────────────────────────────────────────────────────
风格签名 → D:\Engchain3.0\ui-design-runs\聊天窗口DEngchai-20260924-110626-0341\style.json

  ground          （待你定）              底色（暗场 / 亮场 / 高饱和色场 / 纸感）
                  └ query 没给这一维的信号 —— 由你按内容定
  typeVoice       （待你定）              字的性格（无衬线冷静 / 衬线编辑 / 压缩体海报 / 圆体友好 / 美术字）
                  └ query 没给这一维的信号 —— 由你按内容定
  imageWorld      （待你定）              影像世界（实拍 / 3D 渲染 / 插画 / 图形抽象 / 无影像）
                  └ query 没给这一维的信号 —— 由你按内容定
  layoutLogic     （待你定）              版面逻辑（网格 / 中轴 / 拼贴 / 满版叠压 / 长滚动叙事）
                  └ query 没给这一维的信号 —— 由你按内容定
  colorStrategy   （待你定）              配色分工（单色+中性 / 双色对冲 / 三色分工 / 全彩）
                  └ query 没给这一维的信号 —— 由你按内容定
  motionVoice     （待你定）              动的语气（静 / 缓推 / 位移视差 / 逐字入场 / 场景推进）
                  └ query 没给这一维的信号 —— 由你按内容定

有 6 维留空了。**留空不是脚本没算出来，是它拒绝替你猜。**
  这里**不兜底**成「取最近几稿用得最少的那个」，因为历史文件在真实链路里
  永远是空的，于是它退化成「取第一个键」：三条真实 trace 的 ground 全是 dark。
  「校园社交 App 用黑底」就是这么来的 —— 不是审美失误，是默认值。

  ground　底色（暗场 / 亮场 / 高饱和色场 / 纸感）
    dark               线索：夜、暗、科技、游戏、影音、酷、赛博、潮、硬核、音乐
    light              线索：清爽、简洁、医疗、健康、办公、效率、教育、干净、校园、学生
    saturated          线索：活动、促销、节日、年轻、冲击、营销、潮玩、运动、骑行、跑步
    paper              线索：文化、书、杂志、手作、茶、复古、档案、报、展、手账
    → 定了就写回去：python3 C:\Users\36434\AppData\Local\DoubaoWork\User Data\Profile 2\.doubaowork\agent_mode\workspace\.skills\doubao-ui-design\scripts/style.py --query "…" --set ground=<值>

  typeVoice　字的性格（无衬线冷静 / 衬线编辑 / 压缩体海报 / 圆体友好 / 美术字）
    sans-cool          线索：科技、效率、工具、saas、后台、数据、金融
    serif-editorial    线索：文化、杂志、奢、品牌、故事、书、展、酒
    condensed-poster   线索：运动、潮、音乐、球、赛、冲击、街、骑行、跑步、户外
    rounded-friendly   线索：儿童、宠物、社区、亲子、生活、萌、陪伴、校园、学生、论坛
    display-art        线索：活动、年度、盘点、campaign、h5、美术字、国潮
    → 定了就写回去：python3 C:\Users\36434\AppData\Local\DoubaoWork\User Data\Profile 2\.doubaowork\agent_mode\workspace\.skills\doubao-ui-design\scripts/style.py --query "…" --set typeVoice=<值>

  imageWorld　影像世界（实拍 / 3D 渲染 / 插画 / 图形抽象 / 无影像）
    photo              线索：实拍、人物、服饰、食、旅行、房、场景、餐、咖啡、骑行
    render3d           线索：数码、硬件、汽车、材质、产品、工业、3d
    illustration       线索：儿童、教育、故事、插画、宠物、萌、科普
    graphic-abstract   线索：数据、金融、saas、分析、抽象、后台、报表
    none               线索：纯文字、文档、条款、说明
    → 定了就写回去：python3 C:\Users\36434\AppData\Local\DoubaoWork\User Data\Profile 2\.doubaowork\agent_mode\workspace\.skills\doubao-ui-design\scripts/style.py --query "…" --set imageWorld=<值>

  layoutLogic　版面逻辑（网格 / 中轴 / 拼贴 / 满版叠压 / 长滚动叙事）
    grid               线索：商品、列表、目录、商城、作品集、档案
    axial              线索：宣言、品牌、单品、主张、发布
    collage            线索：杂志、潮、青年、文化、拼、街、论坛、信息流、动态、广场
    full-bleed-stack   线索：沉浸、大片、旅行、影音、空间、展
    long-scroll        线索：年度、盘点、故事、叙事、历程、报告、路线、行程、流程、一天
    → 定了就写回去：python3 C:\Users\36434\AppData\Local\DoubaoWork\User Data\Profile 2\.doubaowork\agent_mode\workspace\.skills\doubao-ui-design\scripts/style.py --query "…" --set layoutLogic=<值>

  colorStrategy　配色分工（单色+中性 / 双色对冲 / 三色分工 / 全彩）
    mono-neutral       线索：克制、高级、奢、极简、黑白、性冷淡
    duotone-clash      线索：潮、运动、冲击、年轻、街、对撞
    tri-role           线索：工具、后台、saas、系统、效率、多状态
    full-color         线索：儿童、节日、活动、缤纷、游乐、美食、社交、校园、青春、ugc
    → 定了就写回去：python3 C:\Users\36434\AppData\Local\DoubaoWork\User Data\Profile 2\.doubaowork\agent_mode\workspace\.skills\doubao-ui-design\scripts/style.py --query "…" --set colorStrategy=<值>

  motionVoice　动的语气（静 / 缓推 / 位移视差 / 逐字入场 / 场景推进）
    still              线索：文档、严肃、医疗、政务、条款
    slow-push          线索：品牌、奢、空间、建筑、静
    parallax           线索：官网、landing、产品、故事
    type-in            线索：宣言、主张、文案、标题
    scene-advance      线索：h5、年度、盘点、活动、翻页、一屏
    → 定了就写回去：python3 C:\Users\36434\AppData\Local\DoubaoWork\User Data\Profile 2\.doubaowork\agent_mode\workspace\.skills\doubao-ui-design\scripts/style.py --query "…" --set motionVoice=<值>

与最近 5 稿**都定了值的维度**里的差异：[0, 0, 0, 0, 0] 维（要求 >= 3）
  差异不够，但有语义信号的维度不会被强行换掉 ——
  风格要服从内容。这里请你自己用 --set 换掉一到两维。

这只是方向。**配色、字号、圆角、构图仍然是你写的。**
gold/ 是机制的证据，不是可以照抄的版式：
  允许学 —— 为什么用 2px 墨线做章节分隔、为什么商品缝隙压到 4px。
  禁止抄 —— 四宫格的具体排法、章节编号的具体样式、关键屏网格的具体结构。
  **产物和 gold 在版式上一眼能认出是同一套模板，就是抄了。**

──────────────────────────────────────────────────────────────────────
③  成品骨架（几何已经对了的起点，剩下的归你）
──────────────────────────────────────────────────────────────────────
已拷到 D:\Engchain3.0\ui-design-runs\聊天窗口DEngchai-20260924-110626-0341\app\index.html（351 行）

这份文件现在归你。里面所有 TODO 和占位值都要换掉 —— 留着不换会被 placeholder 门拦下。

动手写之前，这四份读完了吗（各约 1.5 秒）：
  craft/award-mechanisms.md    得奖级的六条机制 —— 你这一稿的对手是它们
  craft/scale-atlas.md         12 份获奖稿的数值区间（字阶 / 圆角 / 饱和度）
  craft/composition-motion.md  构图四条关系 + 滚动驱动 vs 入场揭示
  craft/aesthetic-baseline.md  四组数值的判据
  骨架只保证装得下、不溢出、可读。**好不好看在这四份里。**

  一条实测：常见产物的圆角中位是 50px（全是胶囊），而获奖库中位 3px、12 份里 4 份纯直角。
  大圆角是「默认好看」的安全牌 —— 它让任何东西都不难看，也让任何东西都不特别。

──────────────────────────────────────────────────────────────────────
④  doc 的视觉人格（现在就定，做文档那一步要用）
──────────────────────────────────────────────────────────────────────
这份骨架**不带人格**——带了的话，两份不同项目的文档 13 个视觉角色里会有 7 个相同。

必须给全这 11 个值（--persona k=v,k=v）：
  paper 纸  ink 墨  muted 灰阶  hairline 发丝线  soft 浅底块
  font 字体栈  rule 章节分隔线  numfont 编号字形  radius 圆角
  bodysize 正文字号  lead 行高

本稿签名：ground=None typeVoice=None
→ 方向（值仍然由你定）：

例（三份 gold 的做法，可以看不可以抄）：
  PULSE  paper=#FFFFFF,ink=#14161A,font=系统无衬线,rule=2px solid,radius=3px
  QQ     paper=#F6F2E8,ink=#1B2A4A,font=Songti SC,rule=3px double,radius=0
  骑不骑  paper=#FAF8F4,ink=#3A3530,font=Nunito,rule=none,radius=18px
  它们两两之间最多 5 个角色相同。你这份对它们任何一份也不该超过 5。

══════ 合同 · 每条命令都重印（上下文会被压缩，别靠记）══════════════════
原话  聊天窗口（D:/Engchain3.0/pages/message/…式过于普通，并没有实现优质效果的名片的样式。
合同  mobile-app · revise · 产品界面 · 交 html+doc
风格  （还没跑 style.py）　← 配色/字体/构图按它长，不要凭手感
阶段  产品（app/index.html）
交件① app/index.html        版心在 1440 下必须 <=520px（取景框）· 量 390×844 与 1440×900（卡片被看到的宽度）
交件② doc/*                 画布 2 块 + 封面 + proposal　关键界面节由 ship 自动写入
顺序  先做完①再写② —— ②的关键界面节放的是①的真机截图，不是素材照片
下一步  看图＝**执行** ui.py review（不是自己 open_url/截图/起 http.server）　交付＝ui.py ship 出参数：先 NotifyHuman 交 Doc、再 app_builder_agent 交卡
账本  必须出现：发名片
对手  C:\Users\36434\AppData\Local\DoubaoWork\User Data\Profile 2\.doubaowork\agent_mode\workspace\.skills\doubao-ui-design\gold\app-home\index.html　← 学机制不抄版式（gold/README.md）
盘在  D:\Engchain3.0\ui-design-runs\聊天窗口DEngchai-20260924-110626-0341
══════════════════════════════════════════════════════════════════════════

──────────────────────────────────────────────────────────────────────
NEXT ▸ 采素材 → 亲手写 app/index.html（骨架已就位，直接改它，所有 TODO 和占位值都要换掉）
       写完立刻回来看自己的产物 —— 这一步是整个包最重要的一环。
       **后面每一条命令都从输出里复制，不要凭记忆敲**（它们带 --run）：

    python3 C:\Users\36434\AppData\Local\DoubaoWork\User Data\Profile 2\.doubaowork\agent_mode\workspace\.skills\doubao-ui-design\scripts\ui.py review --run D:\Engchain3.0\ui-design-runs\聊天窗口DEngchai-20260924-110626-0341\run.json
──────────────────────────────────────────────────────────────────────
